CREATE FUNCTION GRADE(MARKS numeric) RETURNS VARCHAR(20)
DETERMINISTIC
BEGIN
DECLARE FMARKS VARCHAR(20);
IF MARKS<33 THEN
SET FMARKS = 'FAIL';
ELSEIF MARKS>33 THEN
SET FMARKS = 'PASS';
END IF;
RETURN(FMARKS);
END;

select product_no,proprice(sell_price) from product_master;
